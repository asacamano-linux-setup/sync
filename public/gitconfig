# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
name = {{ lookup('ansible.builtin.env', 'GIT_NAME', default='nobody') }}
email = {{ lookup('ansible.builtin.env', 'GIT_EMAIL', default='nobody@nowhere.edu') }}
